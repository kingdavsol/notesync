# Database
DATABASE_URL=postgresql://user:password@localhost:5432/notesync

# JWT Secret - CHANGE THIS IN PRODUCTION (use: openssl rand -hex 64)
JWT_SECRET=your-super-secret-jwt-key-change-this-in-production

# Encryption Key - CHANGE THIS IN PRODUCTION (use: openssl rand -hex 32)
ENCRYPTION_KEY=your-encryption-key-for-sensitive-notes

# Server
PORT=3001
NODE_ENV=development

# Frontend URL (for CORS - add your production domain)
FRONTEND_URL=http://localhost:3000

# Security Settings
# Rate limiting is configured in code, adjust in src/index.js if needed
# Session timeout is 7 days by default, adjust in src/middleware/auth.js
